# KB-023 — Memos (app de notas via navegador)

## Status
- **Coluna atual:** Done
- **Prioridade:** Média
- **Criado em:** 2026-04-22

## Descrição

Deploy do Memos — app de notas rápidas self-hosted com interface web completa. Estilo microblog com suporte a markdown, tags e busca. Leve e ideal para o hardware disponível.

## Detalhes Técnicos

| Campo | Valor |
|---|---|
| Imagem | `neosmemo/memos:0.27.1` |
| Porta | 5230 |
| Banco de dados | PostgreSQL (já existente no cluster) |
| Volumes | Nenhum obrigatório (dados no PostgreSQL) |
| Ingress | `https://memos.bisnaguete.xyz` |

### Env vars esperadas

| Variável | Valor |
|---|---|
| `MEMOS_DRIVER` | `postgres` |
| `MEMOS_DSN` | `postgresql://user:password@postgresql.postgresql.svc:5432/memos?sslmode=disable` |

### Manifests a criar

- `apps/memos/deployment.yaml`
- `apps/memos/service.yaml`
- `apps/memos/ingress.yaml`
- `apps/memos/sealed-secret.yaml` (credenciais do PostgreSQL)
- `argocd/apps/memos.yaml`

### Preparação no PostgreSQL

Seguir o mesmo padrão do Joplin — init container com imagem `postgres:17` para criar database e usuário automaticamente:
```sql
CREATE DATABASE memos;
CREATE USER memos WITH PASSWORD 'senha-aqui';
GRANT ALL PRIVILEGES ON DATABASE memos TO memos;
```

## Critérios de Aceite

- [x] Init container cria database `memos` no PostgreSQL automaticamente
- [x] Pod running no namespace `memos`
- [x] Web UI acessível via `https://memos.bisnaguete.xyz`
- [x] Registro de primeiro usuário (admin) funcional via navegador
- [x] Login via SSO do Authentik funcional
- [x] Criar e visualizar notas via interface web
- [x] ArgoCD: Synced + Healthy

## Notas Técnicas

- Memos é muito leve (~50 MB RAM), ideal para o hardware do servidor
- Suporta PostgreSQL nativamente via variável `MEMOS_DRIVER=postgres`
- Registro DNS `memos.bisnaguete.xyz` precisa ser criado no Cloudflare

### Autenticação via Authentik (OIDC nativo)

Memos suporta SSO via OpenID Connect nativamente. Configuração:
1. Criar **Provider** tipo OAuth2/OIDC no Authentik (Client ID: `memos`)
2. Criar **Application** vinculada ao provider
3. Configurar no Memos via Settings → SSO:
   - Tipo: OAuth2
   - Issuer: `https://auth.bisnaguete.xyz/application/o/memos/`
   - Client ID + Client Secret do Authentik
4. Desabilitar registro público — usuários se autenticam via Authentik

Não precisa de Forward Auth no ingress (OIDC é nativo = experiência de login mais fluida).

## Dependências

- KB-010 (PostgreSQL)
- KB-024 (Authentik — OIDC nativo)

## Histórico

| Data       | De  | Para    | Observação  |
|------------|-----|---------|-------------|
| 2026-04-22 | —   | Backlog | Card criado |
| 2026-04-22 | Backlog | To Do | Refinado e priorizado |
| 2026-04-24 | To Do | In Progress | Manifests criados: deployment, service, ingress, sealed-secret, ArgoCD app |
| 2026-04-24 | In Progress | Done | Deploy OK, OIDC Authentik configurado, todos critérios aceitos |
